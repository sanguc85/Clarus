import { FilterSubPipe } from './filter-sub.pipe';

describe('FilterSubPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSubPipe();
    expect(pipe).toBeTruthy();
  });
});
