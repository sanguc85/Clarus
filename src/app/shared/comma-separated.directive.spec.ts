import { CommaSeparatedDirective } from './comma-separated.directive';

describe('CommaSeparatedDirective', () => {
  it('should create an instance', () => {
    const directive = new CommaSeparatedDirective();
    expect(directive).toBeTruthy();
  });
});
