import { FormatPercentageDirective } from './format-percentage.directive';

describe('FormatPercentageDirective', () => {
  it('should create an instance', () => {
    //const directive = new FormatPercentageDirective();
    //expect(directive).toBeTruthy();
  });
});
